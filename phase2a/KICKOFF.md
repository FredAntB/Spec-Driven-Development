# Phase 2A Kickoff

Paste the following into the Code tab to run the static assertions:

---

Run the Phase 2A static assertion suite using your terminal.

Steps:
1. Detect the Python executable (try `python3 --version`, then `python --version`)
2. Run the assertions with UTF-8 encoding enforced:
   - On macOS/Linux: `python3 phase2a/run_assertions.py`
   - On Windows:     `PYTHONUTF8=1 python phase2a/run_assertions.py`
3. Print the full output here
4. If any assertions fail, read the relevant section of SKILL.md or the
   reference files and explain what needs to change to fix each failure
5. If all assertions pass, confirm: "Phase 2A complete: [N]/[N] — ready for 2B"

Do not modify any skill files during this run.
